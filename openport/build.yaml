# The openport release binaries are glibc-linked (CGO/sqlite), so build on the
# Debian base images rather than the default Alpine ones.
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base-debian:bookworm
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
  armv7: ghcr.io/home-assistant/armv7-base-debian:bookworm
  armhf: ghcr.io/home-assistant/armhf-base-debian:bookworm
